// STUB: spec authored by the JS/TS Unit Test Engineer
/**
 * Stub spec for the Access Denied (403) page (`/access-denied`).
 *
 * Cases to cover (real assertions owned by the JS/TS Unit Test Engineer):
 */
import { describe, it } from 'vitest';

describe('/access-denied +page.svelte', () => {
  it.todo('renders the data-testid="access-denied" landmark');
  it.todo('renders the "403" status indicator marked aria-hidden');
  it.todo('renders the "Access denied" heading (level 1)');
  it.todo('renders the permission-denied body copy');
  it.todo('renders a "Back to home" link pointing at root-relative "/"');
  it.todo('marks the decorative shield SVG aria-hidden');
});
