// STUB: spec authored by the JS/TS Unit Test Engineer
/**
 * Stub spec for the new ticket page (`/tickets/new` +page.svelte).
 *
 * Cases to cover (real assertions owned by the JS/TS Unit Test Engineer):
 */
import { describe, it } from 'vitest';

describe('/tickets/new +page.svelte', () => {
  it.todo('renders the data-testid="ticket-new-form" form');
  it.todo('renders title, description, team, and project fields with <label>s');
  it.todo('populates Status and Priority selects from the label maps');
  it.todo('progressively enhances submission via use:enhance');
  it.todo('renders field-level errors from form.errors next to each field');
  it.todo('renders a form-level error for the "" error key');
  it.todo('repopulates fields from form.values after a failed submit');
  it.todo('renders a Cancel link back to /tickets');
});
