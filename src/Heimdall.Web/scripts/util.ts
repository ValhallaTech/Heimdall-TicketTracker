/**
 * Trivial pure-function utilities used to seed the test suite and validate the
 * Jest + ts-jest + coverage pipeline. Replace/extend with real production logic
 * as the JavaScript/TypeScript surface area of Heimdall.Web grows.
 */

export const add = (a: number, b: number): number => a + b;
