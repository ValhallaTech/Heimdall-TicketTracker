# OpenFGA bootstrap output — generated by scripts/openfga-bootstrap.sh.
# Contains store_id and authorization_model_id; operator pastes them into
# Render env vars rather than committing them.
.bootstrap-output.json
